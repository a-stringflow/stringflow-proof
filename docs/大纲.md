# 《5x+1 的 7 发散》论文大纲

> 状态：Lean 形式化处于最后冲刺阶段。论文不得在
> `lake build` 全绿、0 `sorry` / 0 `axiom` 之前宣称主定理闭合；
> 最终权威是 `lean/` 仓库与 CI。

## 0. 论文定位与验证标准

- 主定理：加速 5x+1 轨道 `5XPlusOneOrbit 7` 无界。
- 等价表述：轨道 `7` 不进入任何有限循环。
- Lean 公共声明：`StringFlow.IsUnboundedOrbit 7`。
- 验证边界：`lake build`、`AxiomAudit.lean`、CI zero-sorry gate。

## 1. 引言

- 1.1 5x+1 与加速轨道定义：
  `T(x)=(5x+1)/2^t`，`t=v_2(5x+1)`。
- 1.2 问题动机与相关背景：单点发散、循环排除、3x+1 对照。
- 1.3 本文路线概览：弦流代数 + 三位一体归谬 + Lean 机器核验。
- 1.4 贡献清单：不引入外部文献结论；证书只作归纳基例；
  最终标准 0 `sorry` / 0 自定义公理。

## 2. 弦流代数基础

- 2.1 词、前缀权重 `W`、分子 `wordA`、轨道恒等式
  `2^W·y = 5^j·m + A`。
- 2.2 2-adic rank：`v2(y+1)`；步类型 `t∈{1,2}∪{≥3}`：
  t=1 / t=2 / C3 的精确 rank 自动机。
- 2.3 循环词 `cycleWord c p`、周期闭包、全局最小状态。
- 2.4 rise/C3 循环块分解：`headDepth`、`tailDepth`、
  `suffixWord`、`c3Word`、块电荷与 tail rank。
- 2.5 PMI 与精确恒等式的使用边界（PMI 本身在
  `hglobal_min` 下恒真，不能单独作为矛盾来源）。

## 3. 三位一体归谬总览

- 3.1 三个投影：
  - hterm：真实块首前驱与 reset 结构；
  - unified core：选中块局部窗口上界；
  - hfail：选中块 rank 下界需求。
- 3.2 同一个选中块 `(b, L, hres)`：
  上界 `≤ 2L+11/10`，下界 `≥ 2L+12/11`，差 1 即矛盾。
- 3.3 为什么三路必须同时使用：单条条件欠定；
  13/17 影子只缺 hcycle 锚点。

## 4. hterm：选中块与前驱恒等式

- 4.1 循环 C3→rise 边界与 `IsCyclicC3RiseBoundaryAt`。
- 4.2 真实前驱恒等式
  `5^rt.k·rt.s + delta·5^(L-1) - 1 = wordOrbit u' q`。
- 4.3 从 hpred 到 `ResetHeadEq` / `IsLocalResetTerminal`。
- 4.4 `L ≥ 3` 的结构前提与伪候选排除。

## 5. 统一核心：局部窗口上界

- 5.1 无需全局 `decisiveWindowValuationBoundCorrected`。
- 5.2 `SelectedBlockData_local_window_bounds`：
  选中块尾端 t=1 / t=2 窗口上界。
- 5.3 上界只绑定选中块局部数据，不引入全局预算。

## 6. hfail：等号游程与预算下界

- 6.1 `noLongT2Run` 分支归约为等号游程
  `run.length = run.start + 6`。
- 6.2 等号游程 ⇒ 存在 `j` 满足 `2j+9 ≤ v2(y_{c+j}+1)`。
- 6.3 块层预算 `hfailBudgetLowerBoundAt`：
  `2(tailDepth+1)+11 ≤ 2 + 2N − F`。
- 6.4 求和版下界 `2Σb + 13K ≤ ΣR` 与鸽巢原理。
- 6.5 整循环精确合并：
  - `A_P = m(2^S − 5^P)`；
  - `2^S > 5^P` 的精确使用；
  - 旋转词恒等式 `q_r·Δ = wordA(suffixWord r ++ rest)`；
  - `v2(Δ(q_r+1)) = R_r`（`Δ` 为奇数）；
  - C3 链 residual 与 `c3Chain_rank_gain` 的逐块重排。
- 6.6 hfail 只消费、不产生下界：下界由总层供给。

## 7. 循环桥与主定理

- 7.1 `TrinityBlock` 与 `trinity_block_contradicts`。
- 7.2 `trinityBlockExistsOfCoreValuation` / `...OfComponents`。
- 7.3 `OrbitCycle 7 → False`，从而 `¬ OrbitCycle 7`。
- 7.4 `¬ OrbitCycle 7 → IsUnboundedOrbit 7`。

## 8. Lean 形式化与可复现性

- 8.1 公共声明与 Lean 文件地图：
  `FinalStatement`、`FinalTheorem`、`CycleBridge`、
  `RealOrbitLocalLemma`、`RiseDecompositionAssembly`、
  `amiya`、`closure`、`trinity`。
- 8.2 构建与审计：`lake build`、`AxiomAudit.lean`、CI。
- 8.3 论文与 Lean 的对应表：每个定理名映射到 Lean 声明。
- 8.4 当前剩余义务（写作时）：
  `cycleRiseBlockTailRankSumBound` 与
  `FinalTheorem.five_x_plus_one_diverges_at_7`。

## 9. 相关工作与讨论

- 9.1 循环排除的有限枚举与证书边界。
- 9.2 与 3x+1 对照、启发式与严格证明的区别。
- 9.3 弦流代数作为证明框架的适用范围。
- 9.4 未来工作：常数的紧性、其他单点轨道、泛化族。

## 10. 待办与状态表

| 项目 | 状态 |
|---|---|
| 数学主链（三位一体归谬） | 最后冲刺 |
| `cycleRiseBlockTailRankSumBound` | 待闭合 |
| `hfailBudgetLowerBound` | 鸽巢桥已备 |
| `FinalTheorem` 最终 `sorry` | 待删除 |
| 全量 `lake build` 0 sorry / 0 axiom | 待运行 |
| 论文正式结论 | 待 Lean 全绿后落笔 |

## 附录

- A. 术语表：rank、rise、C3、tailDepth、hres、TrinityCoreValuation。
- B. 文件地图：`paper/sections/*.tex` 与 `lean/*.lean` 的对应。
- C. 提交检查清单：无禁用词、无提前宣称、常量精确、
  Lean 快照可复现。
